# Kernels in kvcomp_cuda_extension folder


# Setup

```
conda create -n kvcomp python=3.12
conda install nvidia/label/cuda-12.4.1::cuda-toolkit
pip install -r requirements.txt
cd kvcomp_cuda_extension
pip install .
```
